bool isPrime(num n) {
    num i := 2
    while i <= (n / 2) {
        if (n % i) == 0 {
            return false
        }
        i := i + 1
    }
    return true 
}

num i := 5
num j := 7
num k := 6

print(bool_to_string(isPrime(i)))
print(bool_to_string(isPrime(j)))
print(bool_to_string(isPrime(k)))